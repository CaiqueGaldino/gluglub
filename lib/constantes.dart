import 'package:flutter/material.dart';

class HydroPalette {
  // Cores primárias (água)
  static const deepOcean = Color(0xFF0066CC);
  static const freshBlue = Color(0xFF3399FF);
  static const crystalTeal = Color(0xFF00CCCC);
  static const mist = Color(0xFF66CCFF);
  static const pureWater = Color(0xFFE6F7FF);
  
  // Cores funcionais
  static const vitalGreen = Color(0xFF33CC99);
  static const sunrise = Color(0xFFFFAA33);
  static const coralAlert = Color(0xFFFF6666);
  
  // Neutros
  static const deepNight = Color(0xFF003366);
  static const cloudWhite = Color(0xFFFFFFFF);
  static const midGray = Color(0xFF78909C);

  // Degradê suave de cinco tons de azul (mais claros)
  static const blueUltraLight = Color(0xFFEAF4FF); // Azul quase branco
  static const blueExtraLight = Color(0xFFD6ECFF); // Azul muito claro
  static const blueLight      = Color(0xFFB3D6FF); // Azul claro suave
  static const blueMid        = Color(0xFF72B7FF); // Azul claro
  static const blueSoft       = Color(0xFF4285F4); // Azul padrão Google (mais escuro, mas suave)



}